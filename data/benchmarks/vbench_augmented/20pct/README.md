# VBench Standard 20% 增强提示词

本目录包含 186 条由 Qwen2.5、seed 42 生成的增强提示词，与 `../../vbench_standard/20pct/prompts.txt` 一一对应。

- 生成使用 `prompts.txt`。
- 输出命名使用 `../../vbench_standard/20pct/prompts.txt`。
- 评测使用 `../../vbench_standard/20pct/full_info.json`。

该数据是可公开复现的增强提示词替代方案，不能证明与 LongLive-2.0 论文未公开的提示词增强逐字一致。
