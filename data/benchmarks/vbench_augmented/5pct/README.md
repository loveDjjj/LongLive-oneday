# VBench Standard 5% 增强提示词

本目录包含 43 条由 Qwen2.5、seed 42 生成的增强提示词，与 `../../vbench_standard/5pct/prompts.txt` 一一对应。

- 生成使用 `prompts.txt`。
- 输出命名使用 `../../vbench_standard/5pct/prompts.txt`。
- 评测使用 `../../vbench_standard/5pct/full_info.json`。

该子集覆盖 VBench 的全部 16 个维度，适合快速回归和方案对比，不应作为完整 VBench 报告。
