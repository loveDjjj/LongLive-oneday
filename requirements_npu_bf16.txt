# Ascend NPU BF16 dependency list.
# Install torch/torchvision/torch_npu from the Ascend/CANN matching stack first.
# Do not install CUDA, NVFP4, TransformerEngine, FlashAttention, or TensorRT packages here.

diffusers==0.31.0
transformers>=4.49.0
tokenizers>=0.20.3
accelerate>=1.1.1
tqdm
imageio
imageio-ffmpeg
omegaconf
einops
av==13.1.0
ftfy
regex
sentencepiece
easydict
peft
opencv-python>=4.9.0.80
pydantic==2.10.6
huggingface_hub[cli]

# Needed for training/data workflows, not required for the default txt-prompt inference.
datasets
wandb
lmdb
matplotlib
scikit-image
pycocotools
