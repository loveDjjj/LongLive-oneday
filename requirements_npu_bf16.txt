# 昇腾 NPU BF16 依赖列表。
# 请先安装与 Ascend/CANN 匹配的 torch、torchvision 和 torch_npu。
# 此环境不要安装 CUDA、NVFP4、TransformerEngine、FlashAttention 或 TensorRT 包。
# SLA 训练的 ascend_triton 后端还需要与 torch_npu/CANN 匹配的昇腾 Triton。
# SLA 推理的稀疏 softmax 分支使用 MindIE-SD 3.0.0 RainFusionAttention；
# 算子最低使用 CANN 8.5.0，线性补偿分支由 PyTorch NPU 算子执行。
# 推荐官方镜像组合 CANN 8.5.1 + torch/torch_npu 2.9.0。MindIE-SD 需从昇腾源
# 单独安装，不要使用要求 CANN 9.0.1 的 dev 分支。

diffusers==0.31.0
transformers>=4.49.0,<5
tokenizers>=0.20.3
accelerate>=1.1.1
tqdm
imageio
imageio-ffmpeg
omegaconf
einops
av==13.1.0
ftfy
regex
sentencepiece
easydict
# 固定已验证版本；项目兼容层会关闭不适用于 Ulysses SP 的 Hugging Face TP 加载分支。
peft==0.19.1
opencv-python>=4.9.0.80
pydantic==2.10.6
huggingface_hub[cli]

# 以下依赖用于训练和数据流程，默认纯文本提示词推理不需要全部安装。
datasets
wandb
lmdb
matplotlib
scikit-image
pycocotools
